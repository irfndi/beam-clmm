// Single source of truth for the CLI entrypoint used by the start/dev/setup/
// backtest npm scripts. Keep in sync with the "bin" wrapper (scripts/beam.sh),
// which execs the same cli/index.ts from the package root.
console.log("cli/index.ts");
